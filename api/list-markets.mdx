---
title: "List Markets"
description: "Get available markets from one or more exchanges"
---

`GET /api/v1/markets`

Returns a paginated array of markets from the Silver cache (ClickHouse) with filtering and pagination.

## Modes

The endpoint operates in two modes:

- **Exchanges mode** — pass `exchanges` to browse markets with optional filters.
- **Parsec IDs mode** — pass `parsec_ids` for direct lookup. No other filters allowed.

## Parameters

<ParamField query="exchanges" type="string">
  Comma-separated exchange IDs: `polymarket`, `kalshi`, `limitless`, `opinion`, `predictfun`. Required unless `parsec_ids` is provided.
</ParamField>

<ParamField query="parsec_ids" type="string">
  Comma-separated parsec IDs for direct lookup (format: `{exchange}:{market_id}`). When provided, no other filters are allowed.
</ParamField>

<ParamField query="status" type="string">
  Filter by market status: `active`, `closed`, or `resolved`.
</ParamField>

<ParamField query="search" type="string">
  Case-insensitive keyword search across title, description, and question.
</ParamField>

<ParamField query="group_id" type="string">
  Filter by source-native exchange event/group ID (exact match).
</ParamField>

<ParamField query="event_id" type="string">
  Filter by canonical Parsec event ID (exact match). Prefer this for cross-exchange event queries.
</ParamField>

<ParamField query="min_volume" type="number">
  Minimum volume filter.
</ParamField>

<ParamField query="min_liquidity" type="number">
  Minimum liquidity filter.
</ParamField>

<ParamField query="limit" type="number" default="100">
  Results per page.
</ParamField>

<ParamField query="cursor" type="string">
  Pagination cursor from a previous response.
</ParamField>

## Response

<ResponseField name="(root)" type="object">
  <Expandable title="properties">
    <ResponseField name="markets" type="Market[]">
      Array of markets matching the query.
      <Expandable title="Market properties">
        <ResponseField name="parsec_id" type="string">
          Primary key: `{exchange}:{native_id}`.
        </ResponseField>
        <ResponseField name="parsec_group_id" type="string">
          Canonical Parsec event ID for cross-exchange grouping.
        </ResponseField>
        <ResponseField name="exchange" type="string">
          Exchange identifier.
        </ResponseField>
        <ResponseField name="exchange_market_id" type="string">
          Native exchange market ID.
        </ResponseField>
        <ResponseField name="exchange_group_id" type="string">
          Source-native exchange event/group ID.
        </ResponseField>
        <ResponseField name="slug" type="string">
          URL-friendly identifier.
        </ResponseField>
        <ResponseField name="url" type="string">
          Direct link to the market on the exchange.
        </ResponseField>
        <ResponseField name="question" type="string">
          Market question.
        </ResponseField>
        <ResponseField name="description" type="string">
          Full description.
        </ResponseField>
        <ResponseField name="rules" type="string">
          Resolution rules.
        </ResponseField>
        <ResponseField name="group_title" type="string">
          Title of the parent event/group.
        </ResponseField>
        <ResponseField name="status" type="string">
          Market status (e.g., `active`, `closed`, `resolved`).
        </ResponseField>
        <ResponseField name="market_type" type="string">
          Market type (e.g., `binary`, `categorical`).
        </ResponseField>
        <ResponseField name="outcome_count" type="number">
          Number of outcomes.
        </ResponseField>
        <ResponseField name="outcomes" type="Outcome[]">
          Array of outcome objects.
          <Expandable title="Outcome properties">
            <ResponseField name="name" type="string">
              Outcome label (e.g., `"Yes"`, `"No"`).
            </ResponseField>
            <ResponseField name="price" type="number">
              Last known price for this outcome.
            </ResponseField>
            <ResponseField name="token_id" type="string">
              Token ID for orderbook subscriptions.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="best_bid" type="number">
          Best bid price.
        </ResponseField>
        <ResponseField name="best_ask" type="number">
          Best ask price.
        </ResponseField>
        <ResponseField name="last_price" type="number">
          Last traded price.
        </ResponseField>
        <ResponseField name="volume_total" type="number">
          Total lifetime volume.
        </ResponseField>
        <ResponseField name="volume_24h" type="number">
          24-hour trading volume.
        </ResponseField>
        <ResponseField name="liquidity" type="number">
          Current liquidity.
        </ResponseField>
        <ResponseField name="open_interest" type="number">
          Current open interest.
        </ResponseField>
        <ResponseField name="min_order_size" type="number">
          Minimum order size in contracts. Varies per market on Polymarket (e.g. 5, 15); typically 1 on Kalshi.
        </ResponseField>
        <ResponseField name="tick_size" type="number">
          Minimum price increment for orders on this market.
        </ResponseField>
        <ResponseField name="created_at" type="string">
          ISO 8601 creation time.
        </ResponseField>
        <ResponseField name="updated_at" type="string">
          ISO 8601 last update time.
        </ResponseField>
        <ResponseField name="end_date" type="string">
          ISO 8601 market close/end time.
        </ResponseField>
        <ResponseField name="event_start_time" type="string">
          ISO 8601 event start time.
        </ResponseField>
        <ResponseField name="condition_id" type="string">
          Condition ID for CTF markets.
        </ResponseField>
        <ResponseField name="xref" type="object">
          Cross-reference data (exchange-specific metadata).
        </ResponseField>
        <ResponseField name="collection_date" type="string">
          ISO 8601 date when this record was collected.
        </ResponseField>
        <ResponseField name="last_collected" type="string">
          ISO 8601 timestamp of last data collection.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pagination" type="object">
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          Markets in this page.
        </ResponseField>
        <ResponseField name="total" type="number">
          Total markets matching filters.
        </ResponseField>
        <ResponseField name="has_more" type="boolean">
          Whether more results are available.
        </ResponseField>
        <ResponseField name="next_cursor" type="string">
          Cursor for the next page.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash
# Browse active Polymarket markets
curl -H "X-API-Key: YOUR_API_KEY" \
  "https://api.parsecapi.com/api/v1/markets?exchanges=polymarket&status=active&limit=10"

# Search across multiple exchanges
curl -H "X-API-Key: YOUR_API_KEY" \
  "https://api.parsecapi.com/api/v1/markets?exchanges=polymarket,kalshi&search=bitcoin&min_volume=1000"

# Canonical event query (cross-exchange abstraction)
curl -H "X-API-Key: YOUR_API_KEY" \
  "https://api.parsecapi.com/api/v1/markets?exchanges=polymarket,kalshi&event_id=ev:us-pres-election-winner-2028"

# Direct lookup by parsec IDs
curl -H "X-API-Key: YOUR_API_KEY" \
  "https://api.parsecapi.com/api/v1/markets?parsec_ids=polymarket:0x1234,kalshi:KXBTC-25DEC"
```
</RequestExample>

<ResponseExample>
```json
{
  "markets": [
    {
      "parsec_id": "polymarket:0x1234abcd",
      "parsec_group_id": "polymarket:group_123",
      "exchange": "polymarket",
      "exchange_market_id": "0x1234abcd",
      "exchange_group_id": "group_123",
      "slug": "will-btc-reach-100k",
      "url": "https://polymarket.com/event/will-btc-reach-100k",
      "question": "Will BTC reach $100k by end of 2025?",
      "description": "Resolves Yes if Bitcoin reaches $100,000...",
      "rules": "Market resolves Yes if...",
      "group_title": "Bitcoin Price Milestones",
      "status": "active",
      "market_type": "binary",
      "outcome_count": 2,
      "outcomes": [
        { "name": "Yes", "price": 0.65, "token_id": "token_yes_123" },
        { "name": "No", "price": 0.35, "token_id": "token_no_456" }
      ],
      "best_bid": 0.64,
      "best_ask": 0.66,
      "last_price": 0.65,
      "volume_total": 500000.0,
      "volume_24h": 12500.0,
      "liquidity": 75000.0,
      "open_interest": 30000.0,
      "min_order_size": 15.0,
      "tick_size": 0.01,
      "created_at": "2025-01-01T00:00:00Z",
      "updated_at": "2025-06-15T12:00:00Z",
      "end_date": "2025-12-31T23:59:59Z",
      "condition_id": "0xcond123"
    }
  ],
  "pagination": {
    "count": 1,
    "total": 150,
    "has_more": true,
    "next_cursor": "1"
  }
}
```
</ResponseExample>

## Pagination

1. Make an initial request without a cursor
2. Check `pagination.has_more` — if `true`, pass `pagination.next_cursor` as `cursor` in the next request
3. Repeat until `has_more` is `false`
